function coeff(p::Polynomial{T}) where T
    return NTuple{ord(p)+1, T}(p.c)
end

function gcd_(a::T, b::T) where T 
    while !iszero(b)
        a, b = b, rem(a, b) 
    end
    if a < 0 
        a=-a
    end
    return a
end
